<Window x:Class="DS4BatteryMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DS4BatteryMonitor_WPF"
        mc:Ignorable="d"
        Title="DS4 Battery Bar" Height="20" Width="80"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize">

    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem x:Name="MenuTopMost" Header="Always on Top" IsCheckable="True" IsChecked="True" Click="MenuTopMost_Click"/>
            <MenuItem x:Name="MenuMinimize" Header="Minimize" Click="MenuMinimize_Click"/>
        </ContextMenu>
    </Window.ContextMenu>
    
    <Border CornerRadius="6" Background="#1E1E1E" BorderBrush="#3C3C3C" BorderThickness="1">
        <Grid MouseDown="Window_MouseDown" Background="Transparent">
            <Grid.OpacityMask>
                <VisualBrush Visual="{Binding ElementName=ClipElement}" />
            </Grid.OpacityMask>

            <Border x:Name="ClipElement" Background="Black" CornerRadius="5"/>

            <Rectangle x:Name="BatteryBar" HorizontalAlignment="Left" Fill="LimeGreen" Width="0"/>

            <TextBlock x:Name="StatusText" Text="?" Foreground="White" FontSize="11" FontWeight="Bold"
                   HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock.Effect>
                    <DropShadowEffect BlurRadius="2" ShadowDepth="0" Color="Black"/>
                </TextBlock.Effect>
            </TextBlock>
        </Grid>
    </Border>
</Window>
